import { ClientProjectModel } from './client-project-model';

describe('ClientProjectModel', () => {
  it('should create an instance', () => {
    expect(new ClientProjectModel()).toBeTruthy();
  });
});
